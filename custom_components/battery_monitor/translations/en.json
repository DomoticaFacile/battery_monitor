{
  "title": "Battery Monitor",
  "config": {
    "step": {
      "user": {
        "title": "Battery Monitor",
        "description": "Monitors all battery entities in Home Assistant.",
        "data": {
          "threshold": "Low battery threshold (%)",
          "critical_threshold": "Critical battery threshold (%)",
          "include_heuristic": "Use heuristic (battery in name/entity_id) in addition to device_class",
          "scan_domains": "Domains to scan (comma-separated, e.g. sensor, binary_sensor)",
          "include_entities": "Include only these entities (multi-select)",
          "exclude_entities": "Exclude these entities (multi-select)",
          "include_patterns": "Include only entity_id matching (fnmatch, comma-separated)",
          "exclude_patterns": "Exclude entity_id matching (fnmatch, comma-separated)",
          "ignore_zero_for_lowest": "Ignore 0% for lowest battery",
          "treat_unavailable_as_zero": "Treat unavailable batteries as 0% (when no recent value is known)",
          "notify_on_zero": "Notify when a 0% value appears"
        }
      }
    },
    "error": {
      "critical_above_low": "The critical threshold must not be higher than the low threshold."
    },
    "abort": {
      "single_instance_allowed": "Only one instance of Battery Monitor is allowed."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Battery Monitor options",
        "data": {
          "threshold": "Low battery threshold (%)",
          "critical_threshold": "Critical battery threshold (%)",
          "include_heuristic": "Use heuristic (battery in name/entity_id) in addition to device_class",
          "scan_domains": "Domains to scan (comma-separated, e.g. sensor, binary_sensor)",
          "include_entities": "Include only these entities (multi-select)",
          "exclude_entities": "Exclude these entities (multi-select)",
          "include_patterns": "Include only entity_id matching (fnmatch, comma-separated)",
          "exclude_patterns": "Exclude entity_id matching (fnmatch, comma-separated)",
          "ignore_zero_for_lowest": "Ignore 0% for lowest battery",
          "treat_unavailable_as_zero": "Treat unavailable batteries as 0% (when no recent value is known)",
          "notify_on_zero": "Notify when a 0% value appears"
        }
      }
    },
    "error": {
      "critical_above_low": "The critical threshold must not be higher than the low threshold."
    }
  }
}
